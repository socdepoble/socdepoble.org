Grok: **Auditoria tècnica del bundle 260912_1421 (verificable, air-gapped)**

Bundle: 502 fitxers, 3.228.101 bytes, `verificat:true`. Contracte d’abast respectat (opcions absents declarades i no crítiques). Tot el material analitzat prové exclusivament del cos del bundle.

### 1. Estat del contracte i absències
- Opcionals absents (declarats, no error): `public/gestoria/index.html`, `public/gestoria/tauler.js`, `public/assets/pedra-seca.css`.
- Fitxers obligatoris presents: `package.json`, `vite.config.js`, `eslint.config.js`, `index.html`.
- Deute congelat existent a `.agents/deute/` (frontmatter, nomenclatura, pedra-seca, promessa, rutes, sollutia, teixit, vocabulari, design-guard, estucat). El deute només pot baixar.

### 2. Frontera d’autenticació (A1–A6)
**Causa arrel detectada al codi:**
- `src/data/oauthRelay.js` defineix `RELAY_PER_DEFECTE` (origen canònic per a `relayOrigin()` i validació de `postMessage`).
- `public/auth/callback.html` conté `ORIGENS_PERMESOS` i referències a orígens `auth.*`.
- El tractor `tractor-frontera-auth.mjs` està dissenyat exactament per a aquesta divergència (Camí 1 fallaria en silenci si els orígens no coincideixen exactament amb `===`).

**Evidència mínima verificable:**
- Qualsevol `https://…auth…` a l’HTML que no coincideixi amb l’origen derivat de `RELAY_PER_DEFECTE` és infracció A1.
- Consultes fluixes (`.some`, `.startsWith`, `.includes`, RegExp) sobre `ORIGENS_PERMESOS` són A2.
- Entrades amb barra final, comodí, path o `http:` no-local són A3.
- `sdp_path` sense validació abans de `location.replace` és A5.

**Correcció mínima:**
1. Unificar a una sola constant (`RELAY_PER_DEFECTE`).
2. Fer que `ORIGENS_PERMESOS` es derivi d’aquesta constant (o s’hi generi en build).
3. Forçar només comparacions per igualtat exacta.
4. Validar `sdp_path` (sense `#`, longitud limitada).
5. Re-executar `node tooling/wiki/tractor-frontera-auth.mjs` (o el gate equivalent) fins a 0 infraccions.

### 3. Esquemes de frontmatter (E1–E4)
Dos validadors coexistents:
- `tooling/wiki/schema.json` (usat pel Reflex / `validatePetorreta`).
- `tooling/wiki/esquema_frontmatter.json` (usat per `tractor-frontmatter.mjs`).

**Causa:** històricament divergents (tipus `petorreta` admès en un i rebutjat en l’altre). El tractor `tractor-esquemes.mjs` existeix precisament per detectar-ho.

**Correcció mínima:**
- Fer coincidir exactament els `enum` de `tipus` i `estat`, i les claus `required`.
- Assegurar que el tipus exigit pel Reflex (`tipus: petorreta`) aparegui als dos enums.
- Re-executar `tractor-esquemes.mjs` fins a 0 divergències.

### 4. Nomenclatura i rutes mortes (N1–N3)
- `tractor-nomenclatura.mjs` detecta segments no-snake_case (N1), rutes `_wiki_de_poble/…` citades al codi que no existeixen al disc (N2) i col·lisions de nom canònic (N3).
- Evidència històrica al propi codi del tractor: 15/19 rutes citades apuntaven a carpetes inexistents; renumeracions (p.ex. `01_ser/03_EQUIP_IA.md`) van deixar enllaços penjats.

**Correcció mínima:**
- Executar amb `--pla` (no escriu) → obtenir mapa de renoms + llista d’enllaços `[[…]]` a reescriure **al mateix commit**.
- Aplicar només el pla atòmic; mai renom parcial.
- Re-baseline del deute només després que N2 i N3 baixin.

### 5. Teixit neuronal i sinapsis
- `teixidor-backlinks.mjs` / `teixidora_sinapsis.mjs` controlen orfes, penjats, illes i etiquetes-falses.
- La teixidora està **fail-closed** (`PROCEDEIX = false`) fins a migració completa a Reflex + pla + rollback.
- Passada estructural (Sinapsis nues, “Tornar a:”, 👉) és segura; la de primera menció al cos té pressupost.

**Correcció mínima:** no activar escriptura fins que el Reflex torni. Mentrestant només dry-run + acta a escriptori.

### 6. Skills i consciència contextual (P1–P9)
`tractor-cognitiu.mjs` cobreix:
- Frontmatter fantasma al cos (P1).
- Noms duplicats / col·lisions de node (P2).
- Guardes del Reflex importades i mai cridades + `bypass` + escriptura directa (P3).
- Escriptura fora de la Canonada (P4).
- Skills inabastables pel RAG (P5).
- Política d’idioma (P6).
- Doble font canònica del Consell (P7).
- Esquema i `triggers_on` destruïts (P8).
- Fitxers acompanyants sense consumidor (P9).

**Correcció mínima priorititzada:**
1. Eliminar delimitadors `---` i camps `name:` / `status: canonic` del cos de les skills (P1).
2. Unificar noms de skill i garantir `triggers_on` al frontmatter (P8).
3. Eliminar o consumir acompanyants orfes (P9).
4. Assegurar que el RAG inclogui `.agents/skills` (P5).

### 7. Altres observacions verificables
- Vite: `strictPort: true`, port 3340, watch ignora `.agents`, `_wiki_de_poble`, etc. Correcte.
- Aturador de `service_role` a `VITE_SUPABASE_ANON_KEY` present i correcte.
- Molts scripts legacy estan tombstonats amb `SDP-LOCK` (fail-closed) — comportament desitjat.
- Online-First explícit (ADR-2026-08-ONLINE-FIRST) i coherent amb el bloc d’identitat de la petorreta.

### Veredicte i accions ordenades (mínimes)
1. **Immediat (bloquejant d’entrada):** unificar relé OAuth + llista blanca (frontera-auth) i esquemes de frontmatter.
2. **Següent:** pla de nomenclatura + reescriptura d’enllaços atòmica.
3. **Skills:** netejar fantasmes de frontmatter i disparadors.
4. **Teixit:** mantenir dry-run fins a Reflex.
5. Re-executar tots els tractors (`tractor-*.mjs`) i actualitzar deute només a la baixa.

Cap dependència nova, cap Tailwind al core, cap toc a dades personals, cap cerca externa. SDP-LOCK activat allà on hi ha risc d’escriptura no canònica.

Ancoratge: [[00_index]] · [[doc_governanca]] · [[ADR-2026-08-ONLINE-FIRST]].
