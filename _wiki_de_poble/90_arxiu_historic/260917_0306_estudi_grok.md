---
type: document
status: esborrany
description: "Veredicte global: 7.8 / 10 — no és un 10"
---
Grok: **Auditoria completada.** Document desat a:

`_wiki_de_poble/04_escriptori/260917_0255_INFORME_auditoria_fase4_fase5_amnèsia.md`

### Veredicte global: **7.8 / 10** — no és un 10

| Àmbit | Nota | Estat |
|-------|------|--------|
| **Fase 4 · Pont Sollutia** | 8.5 | Nucli JWT + `emissorEsperat` + relé PKCE **fail-closed i sòlid**. Fissura: `saneja-callback.mjs` cerca `const ORIGENS_PERMESOS` però el HTML declara `var` → la poda de `localhost` a producció **no s’aplica mai**. |
| **Fase 5 · CSS / Pedra Seca** | 9.0 | `@layer reset, sdp, legacy, components, utilities` correcte; `legat.css` **absent del manifest**; façana única `PedraSeca/index.js` robusta. |
| **Amnèsia Cognitiva** | 5.5 | `reflex_plantilles.mjs` + hook existeixen, però el wrapper **degrada a `injectSteps: []`** (silenci). No hi ha porta de format al tancament del torn → l’agent pot saltar-se la plantilla ISO. |

### Prioritats immediates
1. Alinear el regex de `saneja-callback.mjs` amb `var|const|let` (o canviar el HTML a `const`) + assert post-prebuild «zero localhost».
2. Endurir el preflight: plantilla obligatòria absent o tractor exit 1 → **aturada del torn**, no silenci.
3. Rebut del reflex com a prerequisit del tancament d’acta/petorreta.

Amb (1) i (2) el pont i el flux cognitiu pugen a 9.x. El 10 només arriba quan les portes no es poden saltar ni per amnèsia ni per un desajust de declaració.
