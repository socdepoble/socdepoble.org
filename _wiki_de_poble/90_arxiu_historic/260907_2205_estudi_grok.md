---
tipus: document
estat: esborrany
description: "Auditoria Grok: Transició Fase 3 (B1, B2 i rols globals)"
---
# Auditoria Grok: Transició Fase 3 (B1, B2 i rols globals)

**Data:** 07/09/2026
**Veredicte General:** B1 i B2 són sòlids al SQL del bundle. La Fase 3 és viable, el coll d'ampolla és la coherència front/sessió i les polítiques RLS pendents. Nota: 7.5/10.
**Estimació de temps:** 4-6 dies de treball concentrat (1.5 - 2 setmanes per a eixir a producció totalment net i amb tests).

## Text íntegre de la resposta:

## Veredicte ràpid

| Element | Estat | Risc | Acció |
|---------|-------|------|-------|
| **B1** `list_my_organizations` | Corregit al schema del bundle | Baix | Confirmar desplegament real |
| **B2** `private.ajustos` + `handle_new_user` | Sòlid i fail-closed | Mitjà (config) | Seed obligatori de `poble_per_defecte` |
| **Fase 3** Rols globals + claims | Esquema present, RLS parcial | Mitjà-Alt | Completar polítiques + front |
| **Online-First** | Coherent amb ADR | — | No introduir Online-First |

---

## 1. B1 — `list_my_organizations`
Ja està alineat al bundle (retorna 12 columnes, security invoker, filtra per auth.uid() i tenant).
El defecte de columnes ja no existeix al SQL del bundle. Risc residual si el que està desplegat a Supabase és una versió anterior. Acció: `CREATE OR REPLACE` i verificar.

---

## 2. B2 — `private.ajustos` + `handle_new_user`
El trigger té bones bases (fail-closed, no confia cegament en metadata).
Riscos: 
- Si no hi ha fila `poble_per_defecte`, cap registre funciona. 
- El fallback assigna membership automàtica, la qual cosa pot ser indesitjada si l'usuari vol triar poble després. 
- Cal enviar `rgpd: true` explícitament des del front per a que quede registrat.
Acció mínima: fer l'insert a `private.ajustos` abans de produir.

---

## 3. Fase 3 — Rols globals + claims
Punts febles:
- Falta política UPDATE completa i grants per a organitzacions.
- La transferència de ownership no té seqüència atòmica segura.
- `profiles.visibility` està restringit a `private`.
- Hi ha desalineacions històriques al frontend (storage local vs session). Cal unificar a `sessionStorage` + SDK oficial.

---

## 4. Seguretat global
L'RLS i l'aïllament són correctes. El trigger d'alta és fail-closed. No s'ha detectat risc de destrucció de dades personals amb el SQL actual, per tant no cal activar SDP-LOCK per a aquests canvis, només per a migracions sobre profiles.

---

## 5. Nota de complexitat i estimació de temps
**Score de complexitat: 7.5 / 10**
Estimació: 4-6 dies de treball concentrat per a la Fase 3 mínima viable, i 1.5 - 2 setmanes per a una implementació impecable amb tests i perfil públic.

Recomanació d'ordre:
1. Desplegar B1.
2. Seed B2.
3. RLS organitzacions.
4. Unificar sessió.
5. UI reclamació.
